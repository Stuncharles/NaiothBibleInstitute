rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /registrations/{registrationId} {
      // Allow public read and write access for student registration records
      allow read, write: if true;
    }
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

